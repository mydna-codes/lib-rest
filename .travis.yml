language: java
dist: trusty

jdk:
  - openjdk13

branches:
  only:
    - master
    - prod

cache:
  directories:
    - "$HOME/.m2"

install: echo "Skipping installation..."

script:
  - mvn clean deploy -DskipTests=true --settings .ci/settings.xml

notifications:
  email: false
  slack:
    rooms:
      - mydnacodes:juVpZCZ9ITTxUY5ggVNXwVm0#travis-ci
    on_success: always
    on_failure: always
    template:
      - "Repository %{repository_slug} %{result} build (<%{build_url}|#%{build_number}>) for commit (<%{compare_url}|%{commit}>) on branch %{branch}."
      - "Commit author: %{author}"
      - "Commit message: %{commit_message}"
      - "Execution time: %{duration}"
      - "Message: %{message}"